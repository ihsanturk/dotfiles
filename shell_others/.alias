#!/bin/sh

alias v="nvim"
alias :Q="exit"
alias :q="exit"
alias mv="mv -n"
alias n="nnn -d"
alias vim='nvim'
alias eixt="exit"
alias exti="exit"
alias wp="wikipedia"
alias ec="emacsclient"
# alias open='xdg-open'
alias clear='printf ""'
alias rclear='tput clear'
alias watch='watch -ctn0.5'
alias grep="grep --color=auto"
alias acs="printf '=%.0s' {1..80}"
alias mantitle='apropos . | fzf --height 10'
alias cpwd='pwd | pbcopy -selection clipboard'
alias eusc='nvim ${HOME}/dotfiles/doc/Document/useful_commands.sh'
# alias nk='setxkbmap -option caps:escape; xset r rate 220 40; setxkbmap gb'
alias usc='cat ${HOME}/dotfiles/doc/Document/useful_commands.sh | fzf --height 10'
alias disk2='\
sudo fsck.hfsplus -f /dev/sdb2;\
sudo mount -t hfsplus -o force,rw /dev/sdb2 /mnt'
alias growtopia="wine \
/home/ihsn/.wine/drive_c/users/ihsn/Local\ \
Settings/Application\ Data/Growtopia/Growtopia.exe"

alias shh='\
sudo umount /dev/sdb2 2>/dev/null;\
sudo hdparm -Y /dev/sdb2 1>/dev/null;\
sudo hdparm -Y /dev/sdb 1>/dev/null' 

alias l='ls'
alias n='nnn'
alias rm='trash'
alias ll='ls -lh'
alias la='ls -Ah'
alias ..='cd ..'
alias ...='cd ../..'

## Arch
# alias pu='sudo pacman -Syu'
# alias pl='sudo pacman -Q | dmenu -l 10'
# alias pi='sudo pacman -S'
# alias pru='sudo pacman -R $(pacman -Qdtq)'
# alias pr='sudo pacman -Rcns $(pacman -Q | dmenu -l 10 | sed "s/\s.*//g")'
# alias yi='yay -S'
# alias yu='yay -Syu'
# alias yf='yay -Q | dmenu -l 10'
# alias yr='yay -R $(pacman -Q | dmenu -l 10 | sed "s/\s.*//g")'

# Brew 
alias pf='brew search'
alias pr='brew uninstall'
alias pu='brew update'
alias pi='brew install'

## Ubuntu
# alias pf='apt search'
# alias pr='sudo apt remove'
# alias pu='sudo apt update'
# alias pi='sudo apt install'

## Void
# alias pi='sudo xbps-install'
# alias pf='sudo xbps-query -s'
# alias pl='sudo xbps-query -m'
# alias pr='sudo xbps-remove -R'
# alias pu='sudo xbps-install -Syuv'
