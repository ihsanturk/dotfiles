#!/bin/sh

echo 1 | timeago en &>/dev/null ||
	printf "install timeago package:\n\t\e[32;2m> \e[0m\e[33;2mcargo install timeago\e[0m\n"

timestamp() { date +%s; }

input_file=$1
[ -e $input_file ] &&
	{ echo $(( $(timestamp) - $(date -r $input_file +%s) )) | timeago en; }
