#!/bin/sh
vpn_files="/home/ihsn/Document/vpn"

if ! [ $1 ]; then
	read -p "Which one: " input
	sudo openvpn --config ${vpn_files}/*$input* --auth-user-pass ${vpn_files}/up
else
	sudo openvpn --config ${vpn_files}/*$1* --auth-user-pass ${vpn_files}/up
fi
